<UserControl x:Class="My_book_10.Resources.icons.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:My_book_10.Resources.icons"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <!-- Hue Slider -->
        <Slider x:Name="HueSlider" Minimum="0" Maximum="360" ValueChanged="HueSlider_ValueChanged"/>

        <!-- Gradient Picker -->
        <Border x:Name="GradientPicker" Width="200" Height="200" MouseMove="GradientPicker_MouseMove" MouseDown="GradientPicker_MouseMove">
            <Border.Background>
                <LinearGradientBrush x:Name="GradientBrush" StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#FFFFFF"/>
                    <GradientStop Offset="1" Color="#FF0000"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Selected Color Preview -->
        <Border x:Name="SelectedColorPreview" Width="50" Height="50" CornerRadius="5" BorderBrush="Black" BorderThickness="1" Background="#FFFFFF"/>
    </Grid>
</UserControl>
